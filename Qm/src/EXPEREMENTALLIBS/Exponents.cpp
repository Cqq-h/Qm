#include "Exponents.h"

namespace Qm
{
	//double sqrt(double x)
	//{
	//	double solution = (double)x;

	//	for (double previos = (double)0; (solution > (previos + 0.00001)) || (solution < (previos - 0.00001));)
	//	{
	//		previos = solution;
	//		solution -= ((double)(solution * solution - x) / (double)(2.0 * solution));
	//	}
	//	return solution;
	//}
}